name: Base build & publish workflow

on:
  workflow_call:
    inputs:
      image:
        required: true
        type: string
      dockerfile:
        required: false
        type: string
        default: Dockerfile
      context:
        required: false
        type: string
        default: .
      runs-on:
        required: false
        type: string
        default: arc-runner-set
    secrets:
      docker_username:
        required: true
      docker_access_token:
        required: true

jobs:
  publish:
    runs-on: ${{ inputs.runs-on }}
    steps:
      - name: Check out the repo
        uses: actions/checkout@v4

      - name: Extract metadata (tags, labels) for Docker
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ secrets.docker_username }}/${{ inputs.image }}

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.docker_username }}
          password: ${{ secrets.docker_access_token }}

      - name: Build and push Docker image
        uses: docker/build-push-action@v6
        with:
          context: ${{ inputs.context }}
          file: ${{ inputs.dockerfile }}
          platforms: linux/amd64
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=registry,ref=${{ secrets.docker_username }}/${{ inputs.image }}:latest
          cache-to: type=inline
          # Recent BuildKit/build-push-action versions enable provenance & SBOM
          # attestations by default. That turns the image into a manifest list
          # and breaks `type=inline` + `cache-from` layer reuse, forcing a full
          # multi-GB re-push on every run (build "pushing layers" step takes
          # ~45m instead of ~5m). Keep these off so cache reuse keeps working.
          provenance: false
          sbom: false

      - name: Inspect image
        run: |
          docker buildx imagetools inspect ${{ secrets.docker_username }}/${{ inputs.image }}:${{ steps.meta.outputs.version }}
